#include "main.h"
/**
 *_strlen - Return the length of a string
 *@str: The string to get the length if.
 *
 * Return: The length of @str.
 */
int _strlen(char *s)
{
	int i;
	
	i = 0;
	while (*s != '\0')
	{
		i++
		s++
		}
	return (i); 
}

